import { allPageLoad } from "./main.js";
import { baseURL } from "./main.js";

// data.items.sort((a, b) => a["session-number"] - b["session-number"]);

window.addEventListener("DOMContentLoaded", (event) => {
  allPageLoad();
});

function getCourseSessions() {}
